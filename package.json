{
  "name": "ffmpeg-clip-worker",
  "version": "1.0.0",
  "description": "Tiny HTTP service that trims video clips via ffmpeg and yt-dlp",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
